import { Layout } from '@root/components/layout/Layout.component';
import { LoginContainer } from '@root/containers/login/Login.container';

const LoginPage = () => {
  return (
    <Layout>
      <LoginContainer />
    </Layout>
  )
};

export default LoginPage;
