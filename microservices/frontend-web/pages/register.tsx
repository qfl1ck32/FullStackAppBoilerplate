import { Layout } from '@root/components/layout/Layout.component';
import { RegisterContainer } from '@root/containers/register/Register.container';

const RegisterPage = () => {
  return (
    <Layout>
      <RegisterContainer />
    </Layout>
  )
};

export default RegisterPage;
